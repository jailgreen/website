(function ($) {
  'use strict';

  $ = $ && $.hasOwnProperty('default') ? $['default'] : $;

  /* eslint-disable no-unused-vars */
  /* eslint-enable no-unused-vars */

  /* 
   * @license    https://opensource.org/licenses/BSD-3-Clause New BSD License
   * @copyright  (c) 2017-2018, jailgreen jukka@jahlgren.eu
   *
   */

  (function menu($$$1) {
    // Shell for your plugin code
    $$$1.fn.menu = function menu() {
      // Plugin code
      return this.each(function () {
        // Do something to each item
        $$$1(this).hover(function () {
          $$$1(this).animate({ 'paddingLeft': '+15px' }, 'slow');
        }, function () {
          $$$1(this).animate({ 'paddingLeft': '-15px' }, 'slow');
        });
      });
    };
  })(jQuery);

  {
    // Enable LiveReload
    document.write('<script src="http://' + (location.host || 'localhost').split(':')[0] + ':35729/livereload.js?snipver=1"></script>');
  }

  /*
   * Example 1
   *$(function() {
   *  ...
   *});
   *
   * Example 2
   * $( () => {
   * alert(message);
   * });
   */

  (function ($$$1) {
    if (typeof $$$1 === 'undefined') {
      throw new TypeError('Website\'s JavaScript requires jQuery. ' + 'jQuery must be included before Website\'s JavaScript.');
    }

    var version = $$$1.fn.jquery.split(' ')[0].split('.');
    var maxMajor = 4;
    var minMajor = 3;

    if (version[0] < minMajor || version[0] >= maxMajor) {
      throw new Error('Website\'s JavaScript requires at least jQuery v3.0.0');
    }
  })($);

  $(function () {
    $('.app-menu li').menu();
  });

}(jQuery));
//# sourceMappingURL=website.min.js.map
